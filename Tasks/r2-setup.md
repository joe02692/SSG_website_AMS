# (Superseded) Cloudflare R2 setup

**Not used. This file can be deleted.**

R2 required a credit card to sign up. On reflection it was also the wrong
trade: it would have moved access control out of the database and into
application code, in exchange for storage headroom that automatic expiry makes
unnecessary.

Birth certificates stay in Supabase Storage. See **`Tasks/document-storage.md`**.
